<!-- action-footer 组件 — PANR-19 C5 P0 组件 (ADR-007 §二 #5)
     视觉对齐 P04 / P11 / P12 footer pattern
     Props:
       primaryLabel    主按钮文案（如 "立即开通 · 支付暂未上线"）
       primaryDisabled 主按钮 disabled (default true)
       secondaryLabel  次按钮文案（可选；空则不渲染次按钮）
       secondaryDisabled 次按钮 disabled (default false)
       noBorder        是否隐藏顶部 border (default false)
     Events:
       bind:primary    主按钮点击
       bind:secondary  次按钮点击

     历史变更：
       - 2026-08-30 v2 (P04 CTA bug 修复)：移除 slot 模式，组件内部渲染 button。
         原 slot 模式（父页面 <view slot="primary"><button/></view>）在 WeChat mini-program
         下导致组件 WXSS 无法匹配 slot 内容（slot 内容属父级 scope），出现 CTA 不可见 bug。
         当前 props 模式彻底解决 scope 隔离问题。
     系统 nav 由各使用页的 navigationBarTitleText 提供；本组件提供底部 footer 样式 -->
<view class="action-footer {{noBorder ? 'action-footer--no-border' : ''}}">
  <button
    wx:if="{{primaryLabel}}"
    class="action-footer__primary-btn {{primaryDisabled ? 'action-footer__primary-btn--disabled' : ''}}"
    disabled="{{primaryDisabled}}"
    bindtap="onTapPrimary"
    hover-class="action-footer__primary-btn--hover"
  >{{primaryLabel}}</button>
  <button
    wx:if="{{secondaryLabel}}"
    class="action-footer__secondary-btn {{secondaryDisabled ? 'action-footer__secondary-btn--disabled' : ''}}"
    disabled="{{secondaryDisabled}}"
    bindtap="onTapSecondary"
    hover-class="action-footer__secondary-btn--hover"
  >{{secondaryLabel}}</button>
</view>